<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundleDisplayName</key>
	<string>WeekClip</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!-- Build-time values that must not be committed. Each expands from a build
	     setting declared in project.yml, which defaults to empty so a clean
	     checkout and CI both build; real values are passed on the xcodebuild
	     command line from the superrepo's encrypted ledger. See README
	     "Running against dev". The Supabase anon key is public by construction
	     (weekclip.com serves it to every browser) but the ledger is where it
	     lives, and the debug credentials are neither public nor read by a
	     release build — DebugAutoSignIn is inside #if DEBUG. -->
	<key>WeekclipSupabaseAnonKey</key>
	<string>$(WEEKCLIP_SUPABASE_ANON_KEY)</string>
	<key>WeekclipDebugSignInEmail</key>
	<string>$(WEEKCLIP_DEBUG_SIGN_IN_EMAIL)</string>
	<key>WeekclipDebugSignInPassword</key>
	<string>$(WEEKCLIP_DEBUG_SIGN_IN_PASSWORD)</string>
	<!-- Bundled brand faces (Inter + Space Grotesk, both SIL OFL; licences ship
	     in Resources/Licenses/). iOS does NOT register a font just because it is
	     in the bundle — it registers what this array names, and a font that is
	     present but unlisted resolves to nil at `Font.custom`, which SwiftUI
	     silently renders as San Francisco. That failure looks like "the design
	     didn't land", not like an error, so `BrandAssetsTests` asserts every
	     PostScript name resolves rather than trusting this list.

	     Names are file names, not PostScript names: Xcode flattens resources
	     into the bundle root, so the subdirectory is not part of the value. -->
	<key>UIAppFonts</key>
	<array>
		<string>Inter-Regular.ttf</string>
		<string>Inter-Medium.ttf</string>
		<string>Inter-SemiBold.ttf</string>
		<string>Inter-Bold.ttf</string>
		<string>SpaceGrotesk-Regular.ttf</string>
		<string>SpaceGrotesk-Medium.ttf</string>
		<string>SpaceGrotesk-Bold.ttf</string>
	</array>
	<key>UILaunchScreen</key>
	<dict/>
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
